export type AnexoEmail = { url: string; label?: string; filename?: string };
export function EnviarEmailClienteDialog({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void; [key: string]: unknown }) { if (!open) return null; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><div className="rounded-xl border border-border bg-card p-6"><p className="text-sm">Envio de e-mail disponível pelo módulo de mensagens.</p><button type="button" onClick={() => onOpenChange(false)} className="mt-4 rounded border px-3 py-2 text-sm">Fechar</button></div></div>; }
