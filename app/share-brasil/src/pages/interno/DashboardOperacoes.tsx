import { useCallback, useEffect, useState } from "react";
import { Activity, ArrowRight, Bell, CalendarDays, FileText, Fuel, NotebookPen, Plane, RefreshCw, Users, Wrench } from "lucide-react";
import { Button } from "@/components/ui/button";
import { AcaoRapida, CabecalhoSecao, CartaoKpi, EtiquetaStatus, EstadoVazio, HeroDashboard } from "@/components/dashboard/PrimitivosDashboard";
import { buscarPainelOperacoes, type PainelOperacoesResponse, type SolicitacaoVooInterna } from "@/lib/colaborador-api";

function formatarData(valor: string | null | undefined) { if (!valor) return "—"; const data = new Date(`${valor.slice(0, 10)}T00:00:00`); return Number.isNaN(data.getTime()) ? valor : data.toLocaleDateString("pt-BR"); }
function tomStatus(status: string): "green" | "amber" | "red" | "neutral" { if (status === "aprovada") return "green"; if (status === "reprovada" || status === "cancelada") return "red"; if (status === "pendente") return "amber"; return "neutral"; }
function statusLabel(status: string) { return ({ pendente: "Pendente", aprovada: "Aprovada", reprovada: "Reprovada", cancelada: "Cancelada" } as Record<string, string>)[status] || status; }

export default function DashboardOperacoes({ aoNavegar }: { aoNavegar: (menu: string) => void }) {
  const [dados, setDados] = useState<PainelOperacoesResponse | null>(null);
  const [carregando, setCarregando] = useState(true);
  const [atualizando, setAtualizando] = useState(false);
  const [erro, setErro] = useState<string | null>(null);
  const carregar = useCallback(async (silencioso = false) => { if (silencioso) setAtualizando(true); else setCarregando(true); setErro(null); try { setDados(await buscarPainelOperacoes()); } catch { setErro("Não foi possível carregar os dados reais da operação."); } finally { setCarregando(false); setAtualizando(false); } }, []);
  useEffect(() => { void carregar(); }, [carregar]);
  const resumo = dados?.resumo;
  return <div className="route-enter"><HeroDashboard ambiente="operacoes" title="Bom dia, Camilla" subtitle="Centro de comando · dados reais do backend-share"><div className="hidden items-center gap-2 rounded-lg border border-[#5bbd75]/25 bg-[#5bbd75]/8 px-3 py-2 sm:flex"><span className="pulse-dot h-2 w-2 rounded-full bg-[#5bbd75]" /><span className="text-[10px] font-bold text-[#6bd188]">Operação normal</span></div></HeroDashboard>{erro && <div className="mb-5 rounded-xl border border-[#e77b80]/30 bg-[#e77b80]/10 p-4 text-xs text-[#ed8c90]">{erro}<button type="button" onClick={() => void carregar()} className="ml-2 font-bold underline">Tentar novamente</button></div>}<div className="mb-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><CartaoKpi label="Voos hoje" value={carregando ? "—" : String(resumo?.voos_hoje ?? 0).padStart(2, "0")} detail="Solicitações com data de hoje" icon={<Plane size={16} />} /><CartaoKpi label="Pendências" value={carregando ? "—" : String(resumo?.pendencias ?? 0).padStart(2, "0")} detail="Solicitações aguardando decisão" tone="amber" icon={<Activity size={16} />} /><CartaoKpi label="Reservas abertas" value={carregando ? "—" : String(resumo?.reservas_abertas ?? 0)} detail="Agendamentos a partir de hoje" tone="violet" icon={<CalendarDays size={16} />} /><CartaoKpi label="Aeronaves ativas" value={carregando ? "—" : String(resumo?.aeronaves_ativas ?? 0)} detail="Disponíveis na base operacional" tone="green" icon={<Wrench size={16} />} /></div><section className="mb-5 grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-6"><AcaoRapida icon={<CalendarDays size={16} />} label="Agendamentos" detail={`${resumo?.pendencias ?? 0} pendentes`} onClick={() => aoNavegar("agendamentos")} /><AcaoRapida icon={<FileText size={16} />} label="Plano de voo" detail="Abrir módulo" color="violet" onClick={() => aoNavegar("plano-de-voo")} /><AcaoRapida icon={<NotebookPen size={16} />} label="Diário de bordo" detail="Abrir módulo" color="green" onClick={() => aoNavegar("diario-de-bordo")} /><AcaoRapida icon={<Fuel size={16} />} label="Abastecimentos" detail="Abrir módulo" color="amber" onClick={() => aoNavegar("abastecimentos")} /><AcaoRapida icon={<Wrench size={16} />} label="CTM" detail="Abrir módulo" color="green" onClick={() => aoNavegar("ctm")} /><AcaoRapida icon={<Users size={16} />} label="Tripulação" detail="Escalas do dia" color="violet" onClick={() => aoNavegar("tripulacao")} /></section><section className="overflow-hidden rounded-xl border border-border bg-card/75"><CabecalhoSecao icon={<Activity size={15} />} title="Solicitações e voos programados" detail="Atualização direta do endpoint interno" action={<Button type="button" variant="outline" onClick={() => void carregar(true)} disabled={atualizando} className="h-8 gap-1.5 border-border bg-card px-2.5 text-[10px]"><RefreshCw size={12} className={atualizando ? "animate-spin" : ""} /> Atualizar</Button>} />{carregando ? <div className="space-y-3 p-5"><div className="skeleton h-12 rounded-lg" /><div className="skeleton h-12 rounded-lg" /><div className="skeleton h-12 rounded-lg" /></div> : dados?.solicitacoes.length ? <div className="overflow-x-auto"><table className="w-full min-w-[730px] text-left"><thead><tr className="border-b border-border text-[9px] font-bold uppercase tracking-[.11em] text-muted-foreground"><th className="px-4 py-3">Cliente</th><th className="px-4 py-3">Rota</th><th className="px-4 py-3">Data</th><th className="px-4 py-3">Aeronave</th><th className="px-4 py-3">Passageiros</th><th className="px-4 py-3">Status</th><th className="px-4 py-3 text-right">Ação</th></tr></thead><tbody>{dados.solicitacoes.slice(0, 12).map((item) => <LinhaVoo key={item.id} item={item} onOpen={() => aoNavegar("agendamentos")} />)}</tbody></table></div> : <EstadoVazio label="Nenhuma solicitação futura encontrada" />}</section><div className="mt-5 grid gap-5 md:grid-cols-3"><Resumo titulo="Solicitações pendentes" valor={carregando ? "—" : String(resumo?.pendencias ?? 0)} detalhe="Requerem decisão operacional" onClick={() => aoNavegar("agendamentos")} /><Resumo titulo="Aeronaves ativas" valor={carregando ? "—" : String(resumo?.aeronaves_ativas ?? 0)} detalhe="Consulta baseada no cadastro D1" onClick={() => aoNavegar("ctm")} /><Resumo titulo="Data de referência" valor={dados ? formatarData(dados.data_referencia) : "—"} detalhe="Última atualização solicitada" onClick={() => void carregar(true)} /></div></div>;
}
function LinhaVoo({ item, onOpen }: { item: SolicitacaoVooInterna; onOpen: () => void }) { return <tr className="border-b border-border/60 last:border-0 hover:bg-secondary/20"><td className="px-4 py-3"><p className="max-w-[180px] truncate text-[10px] font-bold">{item.cliente_razao_social || item.codigo_cliente || "Cliente não informado"}</p><p className="mt-1 font-mono text-[9px] text-muted-foreground">{item.codigo_cliente || item.id.slice(0, 8)}</p></td><td className="px-4 py-3 font-mono text-[10px] text-muted-foreground">{item.origem} → {item.destino}</td><td className="px-4 py-3"><p className="text-[10px] font-semibold">{formatarData(item.data_agendada)}</p><p className="mt-1 font-mono text-[9px] text-muted-foreground">{item.horario_previsto_agendamento || "A definir"}</p></td><td className="px-4 py-3 font-mono text-[10px] font-bold">{item.matricula_registro || "A definir"}</td><td className="px-4 py-3 font-mono text-[10px] text-muted-foreground">{item.numero_passageiros}</td><td className="px-4 py-3"><EtiquetaStatus tone={tomStatus(item.status)}>{statusLabel(item.status)}</EtiquetaStatus></td><td className="px-4 py-3 text-right"><button type="button" onClick={onOpen} className="text-[10px] font-bold text-primary hover:underline">Abrir</button></td></tr>; }
function Resumo({ titulo, valor, detalhe, onClick }: { titulo: string; valor: string; detalhe: string; onClick: () => void }) { return <section className="rounded-xl border border-border bg-card/75 p-4"><p className="text-[10px] font-bold uppercase tracking-[.08em] text-muted-foreground">{titulo}</p><p className="mt-4 font-mono text-2xl tracking-[-.05em]">{valor}</p><p className="mt-1 text-[10px] text-muted-foreground">{detalhe}</p><button type="button" onClick={onClick} className="mt-4 flex items-center gap-1 text-[10px] font-bold text-primary hover:underline">Abrir módulo <ArrowRight size={12} /></button></section>; }
